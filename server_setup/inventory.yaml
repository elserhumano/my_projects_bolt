---
groups:
- name: common-group
  groups:
  - name: prometeo
    targets:
    - 192.168.167.151
    #config:
    #  ssh:
    #    user: root
    #    port: 22
    #    private-key: ~/.ssh/id_rsa

# General
config:
  transport: ssh
  ssh:
    user: root
    port: 22
    privkey: ~/.ssh/id_rsa 
    #host-key-check: false

