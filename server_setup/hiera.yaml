# hiera.yaml v5
---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data


hierarchy:

  - name: "Add ssh key ansiblix"
    path: "adm_ansiblix.yaml"

  - name: "Mgmt Puppet"
    path: "mgmt_puppet.yaml"

  - name: "Common data for server_setup"
    path: "common.yaml"

